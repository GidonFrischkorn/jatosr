% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata-local.R
\name{jatos_cache_status}
\alias{jatos_cache_status}
\title{Summarise a local cache batch by batch}
\usage{
jatos_cache_status(path, batch_id = NULL)
}
\arguments{
\item{path}{The cache directory, one \verb{batch_<id>} directory inside it, or
the path of a single \code{metadata.json} file.}

\item{batch_id}{Optional ids to keep only some batches.}
}
\value{
A tibble with one row per \code{metadata.json}: \code{batch_id} (\code{NA} when
the file holds several batches), \code{path} (its directory), \code{n_results}
(component results listed), \code{n_downloaded} (with a local file),
\code{n_pending} (server size above \code{0} and no local file, or a local file
smaller than the server's), \code{n_shrunk} (local file larger than the
server's), \code{n_orphans} (data files without a metadata row) and \code{bytes}
(size of the local data files). With \code{batch_id}, the counts cover the
rows of those batches and a \code{metadata.json} without any of them is left
out; \code{n_orphans} still compares the directory with every row of its
\code{metadata.json}, so the files of another batch listed there are not
orphans.
}
\description{
Reads every \code{metadata.json} of a cache, as \code{\link[=jatos_read_metadata]{jatos_read_metadata()}} does,
and reports per batch how many component results it lists, how many are
on disk, how many a run of \code{\link[=jatos_download_results]{jatos_download_results()}} would fetch, and
how many \code{data.txt} files sit in the batch directory without a metadata
row (orphans, counted inside that batch directory only; a fresh download
of the batch's metadata covers them). No request is made.
}
\examples{
cache <- system.file("extdata", "JATOS_data", package = "jatosr")
jatos_cache_status(cache)
jatos_cache_status(cache, batch_id = 34)
}
