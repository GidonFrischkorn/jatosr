% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/result-files.R
\name{jatos_download_files}
\alias{jatos_download_files}
\title{Download the files uploaded during runs into the cache}
\usage{
jatos_download_files(
  metadata,
  path,
  incremental = TRUE,
  chunk_size = 50,
  overwrite = FALSE,
  dry_run = FALSE,
  conn = jatos_connection()
)
}
\arguments{
\item{metadata}{A metadata tibble with the \code{files} column, from
\code{\link[=jatos_results_metadata]{jatos_results_metadata()}} or \code{\link[=jatos_read_metadata]{jatos_read_metadata()}}. A tibble that
\code{\link[=jatos_result_files]{jatos_result_files()}} already widened is not accepted; pass the
metadata.}

\item{path}{The cache directory. Created when missing.}

\item{incremental}{If \code{FALSE}, every component result with a server size
above \code{0} is fetched again, except \code{shrunk} ones unless \code{overwrite = TRUE}.}

\item{chunk_size}{Component results per request.}

\item{overwrite}{If \code{TRUE}, \code{shrunk} rows are fetched and replaced.}

\item{dry_run}{If \code{TRUE}, no request is made and nothing is written: the
rows that a real run would fetch get status \code{pending}, and a message
per batch reports their count, their size on the server and the number
of requests. \code{conn} is not needed.}

\item{conn}{A \code{\link[=jatos_connection]{jatos_connection()}}.}
}
\value{
The tibble of \code{\link[=jatos_result_files]{jatos_result_files()}} with \code{file} (local path,
\code{NA} when absent after the run), \code{file_size} (bytes, \code{NA} when absent)
and \code{status} (\code{fetched}, \code{unchanged}, \code{empty}, \code{shrunk}, \code{missing},
\code{failed}, or \code{pending} in a dry run) added. With \code{dry_run = TRUE} the
same tibble says, without a request, which files are on disk.
}
\description{
Fetches every file listed by \code{\link[=jatos_result_files]{jatos_result_files()}} that is not yet on
disk, or that has grown on the server, and stores it next to the result
data as
\verb{<path>/batch_<batch_id>/study_result_<id>/comp-result_<id>/files/<filename>},
which is the layout the server uses inside its result zips. The rule is
the one of \code{\link[=jatos_download_results]{jatos_download_results()}}, applied per file: a file is
fetched when it is missing locally and has a size above \code{0} on the
server, or when the server's copy is larger; equal sizes are
\code{unchanged}; a server copy that is \emph{smaller} than the local file leaves
the local file alone with status \code{shrunk} unless \code{overwrite = TRUE}. A
file whose size the metadata does not give counts as \code{0}, so it is not
fetched, and a warning names it.
}
\details{
Files are requested from \code{POST /results/files} with the component result
ids in the body, up to \code{chunk_size} component results per request (the
files of one component result always travel in one request), one zip per
chunk, with a progress bar. Only the files that were planned are taken
out of a zip. A planned file that the server's answer does not contain
has status \code{missing}; a request that fails marks its files \code{failed} and
the run goes on, with one warning at the end; after a \code{401} or \code{403} no
further request is made. Every batch that received files gets a fresh
\code{metadata.json}, as in \code{\link[=jatos_download_results]{jatos_download_results()}}.
}
\examples{
\dontrun{
# needs a JATOS server and a stored API token
meta <- jatos_results_metadata(study_id = 12)
jatos_download_files(meta, "JATOS_data", dry_run = TRUE)
files <- jatos_download_files(meta, "JATOS_data")
files[files$status == "fetched", c("component_result_id", "filename", "file")]
}
}
\seealso{
\code{\link[=jatos_result_files]{jatos_result_files()}}, \code{\link[=jatos_download_results]{jatos_download_results()}}.
}
