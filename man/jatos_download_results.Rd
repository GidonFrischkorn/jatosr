% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/results-download.R
\name{jatos_download_results}
\alias{jatos_download_results}
\title{Download result data into a local cache}
\usage{
jatos_download_results(
  metadata,
  path,
  incremental = TRUE,
  chunk_size = 50,
  overwrite = FALSE,
  dry_run = FALSE,
  conn = jatos_connection()
)
}
\arguments{
\item{metadata}{A metadata tibble from \code{\link[=jatos_results_metadata]{jatos_results_metadata()}}. A
tibble from \code{\link[=jatos_read_metadata]{jatos_read_metadata()}} carries the sizes of the cached
\code{metadata.json}, so it finds nothing new; fetch fresh metadata first.}

\item{path}{The cache directory. Created when missing.}

\item{incremental}{If \code{FALSE}, every component result with a server size
above \code{0} is fetched again, except \code{shrunk} ones unless \code{overwrite = TRUE}.}

\item{chunk_size}{Component result ids per request.}

\item{overwrite}{If \code{TRUE}, \code{shrunk} rows are fetched and replaced.}

\item{dry_run}{If \code{TRUE}, no request is made and nothing is written: the
rows that a real run would fetch get status \code{pending}, and a message
per batch reports their count, their size on the server and the number
of requests. \code{conn} is not needed.}

\item{conn}{A \code{\link[=jatos_connection]{jatos_connection()}}.}
}
\value{
\code{metadata} with three columns added (replaced when present):
\code{file} (path of the local \code{data.txt}, \code{NA} when absent after the run),
\code{file_size} (its size in bytes, \code{NA} when absent) and \code{status} (one of
\code{fetched}, \code{unchanged}, \code{empty}, \code{shrunk}, \code{missing}, \code{failed}, or
\code{pending} in a dry run).
}
\description{
Fetches the \code{data.txt} of each component result in \code{metadata} that is not
yet on disk, or that has grown on the server, and stores it under
\verb{<path>/batch_<batch_id>/study_result_<id>/comp-result_<id>/data.txt}. The
comparison is in raw bytes on both sides: \code{data_size} from the metadata
against \code{file.size()} of the local file. Component results are requested
from \code{POST /results/data} in chunks of \code{chunk_size} ids, one zip per
chunk, with a progress bar over the chunks, and every batch that received
data gets a fresh \code{metadata.json} afterwards (one \code{POST /results/metadata}
per batch) so that \code{\link[=jatos_read_metadata]{jatos_read_metadata()}} can rebuild the tibble offline
later. That \verb{batch_<id>/} layout is the only one the package reads or
writes: a directory holding a \code{metadata.json} at its top level or
\verb{JATOS_DATA_<id>} folders (what smartr left behind) is refused, since the
server is the source of truth and the data is downloaded again into a
fresh directory; a zip exported from the JATOS GUI is imported with
\code{\link[=jatos_import_results]{jatos_import_results()}}.
}
\details{
The incremental rule is asymmetric:
\itemize{
\item server size \code{0}: nothing is requested, status \code{empty}, whether or not
a 0-byte local file exists (a larger local file makes it \code{shrunk}, see
below);
\item no local file and server size above \code{0}, or server larger than local:
fetched, status \code{fetched};
\item equal sizes above \code{0}: status \code{unchanged}, no request;
\item server \emph{smaller} than local: the local file stays, status \code{shrunk},
because it may be the last copy of that participant's data. Only
\code{overwrite = TRUE} replaces it.
}

A row that was requested but not part of the server's answer has status
\code{missing} and a warning names it. A request that fails (a transport
error, an HTTP error, an answer that is not a zip file, or a zip that
cannot be unpacked in full) does not stop
the run: its rows get status \code{failed}, the remaining chunks are fetched,
and one warning at the end reports the count and the first error. After
a \code{401} or \code{403} no further request is made, since every one of them
would fail the same way. Files are written to a temporary name inside
the batch directory and renamed into place, so a partial download never
sits under the real name.

Files that participants uploaded during a run (drawings, recordings) are
not part of the result data; \code{\link[=jatos_download_files]{jatos_download_files()}} fetches them with
the same rule, file by file.
}
\examples{
\dontrun{
# needs a JATOS server and a stored API token
meta <- jatos_results_metadata(study_id = 12)
jatos_download_results(meta, "JATOS_data", dry_run = TRUE)   # what would be fetched
meta <- jatos_download_results(meta, "JATOS_data")
table(meta$status)
}
}
\seealso{
\code{\link[=jatos_read_metadata]{jatos_read_metadata()}} to rebuild the metadata from the cache,
\code{\link[=jatos_cache_status]{jatos_cache_status()}} to summarise it, \code{\link[=jatos_read_results]{jatos_read_results()}} to read
the downloaded files, \code{\link[=jatos_download_files]{jatos_download_files()}} for uploaded files.
}
