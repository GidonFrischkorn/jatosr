% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/results-metadata.R
\name{jatos_results_metadata}
\alias{jatos_results_metadata}
\title{Result metadata, one row per component result}
\usage{
jatos_results_metadata(
  study_id = NULL,
  batch_id = NULL,
  component_id = NULL,
  study_result_id = NULL,
  component_result_id = NULL,
  group_id = NULL,
  conn = jatos_connection()
)
}
\arguments{
\item{study_id, component_id}{Ids (integer) or uuids (string) to select
results by, one kind per argument; \code{NULL} (the default) means no
restriction on that dimension. Uuids go to the server as \code{studyUuids}
and \code{componentUuids}.}

\item{batch_id, study_result_id, component_result_id, group_id}{Ids to select results by. \code{NULL} (the default) means no restriction on
that dimension. The server takes no uuid for these.}

\item{conn}{A \code{\link[=jatos_connection]{jatos_connection()}}.}
}
\value{
A metadata tibble, one row per component result, with the
columns documented in \code{\link[=jatos_flatten_metadata]{jatos_flatten_metadata()}}.
}
\description{
Calls \code{POST /results/metadata} with the ids as a JSON body, so any number
of ids fits in one request. Results the token cannot see are left out by
the server without an error. The answer is flattened with
\code{\link[=jatos_flatten_metadata]{jatos_flatten_metadata()}}; see there for the columns.
}
\details{
Every id argument accepts one or more integer ids; \code{study_id} and
\code{component_id} also accept uuid strings, which survive a study's export
and import on another server while its id changes. At least one must be
given; several are combined as the server combines them (the union of
everything selected). A message reports the host, the number of study
results and component results, and how many component results are not
\code{FINISHED}; silence it with \code{options(rlib_message_verbosity = "quiet")}
or \code{suppressMessages()}.
}
\examples{
\dontrun{
# needs a JATOS server and a stored API token
meta <- jatos_results_metadata(study_id = 12)
meta[meta$component_state == "FINISHED" & meta$data_size > 0, ]
}
}
\seealso{
\code{\link[=jatos_study_results]{jatos_study_results()}} for one row per study result,
\code{\link[=jatos_read_metadata]{jatos_read_metadata()}} to rebuild the same tibble from a local cache.
}
