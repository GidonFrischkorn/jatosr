% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-results.R
\name{jatos_read_json}
\alias{jatos_read_json}
\title{Read one jsPsych result file}
\usage{
jatos_read_json(file, flatten = FALSE)
}
\arguments{
\item{file}{Path of the file.}

\item{flatten}{If \code{TRUE}, nested objects become columns named
\code{outer.inner} (see \code{jsonlite::fromJSON()}); otherwise they stay nested
data-frame columns when every trial of the file carries the same
object, and list columns otherwise. \code{\link[=jatos_read_results]{jatos_read_results()}} stores both
as list columns before it binds files.}
}
\value{
A tibble. A key that is the empty string (an unnamed form input)
becomes a column \verb{unnamed_<position>}.
}
\description{
Parses a \code{data.txt} written by jsPsych (a JSON array of trial objects)
into a tibble with one row per trial. A file that holds several JSON
values back to back, which is what repeated \code{jatos.appendResultData()}
calls leave behind (arrays after arrays, or one object per call), is
split into its top-level values first and read as one array. An empty
file gives a tibble with no rows and no columns.
}
\examples{
file <- system.file("extdata", "data.txt", package = "jatosr")
trials <- jatos_read_json(file)
trials[, c("trial_index", "trial_type", "rt", "correct")]
jatos_read_json(file, flatten = TRUE)[1, ]
}
\seealso{
\code{\link[=jatos_read_results]{jatos_read_results()}} to read every file of a metadata tibble.
}
