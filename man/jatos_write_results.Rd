% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write-results.R
\name{jatos_write_results}
\alias{jatos_write_results}
\title{Write a trials tibble to disk}
\usage{
jatos_write_results(
  x,
  file,
  ...,
  format = NULL,
  object = "trials",
  split = c("none", "component"),
  overwrite = FALSE
)
}
\arguments{
\item{x}{A data frame, usually from \code{\link[=jatos_read_results]{jatos_read_results()}}.}

\item{file}{Path of the file to write. With \code{split = "component"} it is
the pattern: \code{results.rds} becomes \verb{results_component_<id>.rds}.}

\item{...}{Must be empty.}

\item{format}{\code{"rds"}, \code{"csv"}, \code{"csv.gz"}, \code{"tsv"}, \code{"parquet"} or
\code{"rdata"}. \code{NULL} (the default) infers it from the extension of
\code{file} (\code{.RData} and \code{.rda} are \code{"rdata"}).}

\item{object}{For \code{RData}: the name of the object in the file, so that
\code{load()} is predictable; a syntactic name (see \code{\link[=make.names]{make.names()}}). With
\code{split = "component"} each file holds \verb{<object>_component_<id>}, the
suffix of its file name.}

\item{split}{\code{"none"} writes one file. \code{"component"} writes one file per
distinct \code{component_id} in \code{x}, with the id in the file name, for
studies whose components have different columns.}

\item{overwrite}{If \code{TRUE}, existing files are replaced.}
}
\value{
The path(s) written, invisibly.
}
\description{
Saves the tibble that \code{\link[=jatos_read_results]{jatos_read_results()}} returns (or any data frame)
as \code{.rds}, \code{.csv}, \code{.csv.gz}, \code{.tsv}, \code{.parquet} or \code{.RData} in one
call. The format is taken from the file extension or from \code{format} when
the file has no extension; a \code{format} that contradicts the extension is
an error, the file is not renamed. The file is written under a temporary
name in the same directory and renamed into place, and an existing file
is never replaced unless \code{overwrite = TRUE}.
}
\details{
\code{rds} and \code{RData} keep every column as it is, list columns included.
\code{parquet} (through the arrow package, which must be installed) reads
from Python and other tools; it keeps a list column or nested
data-frame column when arrow can give it one type, and serialises a
column it cannot type (cells of different shapes, such as an object in
one trial and a vector in the next) to JSON strings the way csv does,
with a message naming the columns. \code{csv}, \code{csv.gz} (the same,
gzip-compressed) and \code{tsv} are written
in UTF-8 without row names, \code{NA} as an empty field, \code{POSIXct} columns as
ISO 8601 in UTC (\verb{2025-08-24T01:46:40Z}), and every list column or
nested data-frame column serialised cell by cell to a JSON string with
\code{jsonlite::toJSON(auto_unbox = TRUE)}; a message names the columns that
were serialised. Read such a cell back with \code{jsonlite::fromJSON()}, or
read the trials with \code{flatten = TRUE} to get nested objects as columns
before writing.
}
\examples{
cache <- system.file("extdata", "JATOS_data", package = "jatosr")
trials <- jatos_read_results(jatos_read_metadata(cache))

out <- tempfile("jatosr-example-")
dir.create(out)
jatos_write_results(trials, file.path(out, "study12.rds"))
jatos_write_results(trials, file.path(out, "study12.csv"))
jatos_write_results(trials, file.path(out, "study12.RData"), object = "study12")
list.files(out)
unlink(out, recursive = TRUE)
}
\seealso{
\code{\link[=jatos_export_results]{jatos_export_results()}} for metadata, download, read and write
in one call.
}
