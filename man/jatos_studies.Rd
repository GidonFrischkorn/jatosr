% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/studies.R
\name{jatos_studies}
\alias{jatos_studies}
\title{List the studies a token can see}
\usage{
jatos_studies(
  with_components = TRUE,
  with_batches = TRUE,
  conn = jatos_connection()
)
}
\arguments{
\item{with_components, with_batches}{Include full component and batch
properties (\code{TRUE}, the default) or only their ids and uuids.}

\item{conn}{A \code{\link[=jatos_connection]{jatos_connection()}}.}
}
\value{
A tibble with one row per study: \code{study_id}, \code{study_uuid},
\code{title}, \code{description}, \code{active}, \code{locked}, \code{group_study},
\code{linear_study}, \code{allow_preview}, \code{dir_name}, \code{end_redirect_url},
\code{members} (list of usernames), \code{components} and \code{batches} (list columns
of tibbles in the shape returned by \code{\link[=jatos_components]{jatos_components()}} and
\code{\link[=jatos_batches]{jatos_batches()}}).
}
\description{
Calls \code{GET /studies/properties}. Each row is one study; its components
and batches are nested as list columns of tibbles, so
\code{tidyr::unnest(studies, batches)} gives one row per batch.
}
\examples{
\dontrun{
# needs a JATOS server and a stored API token
studies <- jatos_studies()
studies[grepl("Binding", studies$title), ]
}
}
