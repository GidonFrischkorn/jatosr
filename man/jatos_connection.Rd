% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection.R
\name{jatos_connection}
\alias{jatos_connection}
\title{Connect to a JATOS server}
\usage{
jatos_connection(
  profile = Sys.getenv("JATOS_PROFILE", "default"),
  host = NULL,
  token = NULL
)
}
\arguments{
\item{profile}{Name of the credential profile, as given to
\code{\link[=jatos_set_credentials]{jatos_set_credentials()}}. Defaults to the \code{JATOS_PROFILE} environment
variable, or \code{"default"} when that is unset, so a single-account setup
never passes it. Case-insensitive.}

\item{host}{Base URL of the JATOS server, e.g. \code{"https://jatos.example.org"}.
Defaults to the profile's host.}

\item{token}{Personal access token. Defaults to the profile's stored
token. Kept for this connection and for this session only.}
}
\value{
An object of class \code{jatos_connection} with elements \code{profile},
\code{host}, \code{api_url}, \code{id}, \code{auth_from} and \code{user_agent}. There is no
\code{token} element.
}
\description{
Builds the connection object that every API function takes as its \code{conn}
argument. With no arguments, the host and token of the active credential
profile are looked up: the host from \code{JATOS_HOST} (or
\verb{JATOS_HOST_<PROFILE>}) or the profile configuration file, the token from
\code{JATOS_TOKEN} (or \verb{JATOS_TOKEN_<PROFILE>}) or the system credential store
(see \code{\link[=jatos_set_credentials]{jatos_set_credentials()}} and \code{\link[=jatos_credentials_sitrep]{jatos_credentials_sitrep()}}). A named
profile is how you keep tokens for several accounts on one server, or for
several servers, side by side. Explicit \code{host} and \code{token} arguments take
precedence over both.
}
\details{
Everything is resolved here rather than at the first request, so a missing
or unreadable token is an error where the connection is built, and a
credential store that has to be unlocked asks once. When
\code{\link[=jatos_token_info]{jatos_token_info()}} has seen that the token expires within seven days,
the next \code{jatos_connection()} warns once per session.
}
\section{The connection holds no token}{

\code{conn} carries the profile name, the host and an opaque session id — not
the secret. The token itself stays in this R session, in an environment
the package owns, and is fetched only inside the request builder. So
\code{saveRDS(conn)}, \code{save.image()}, an \code{.RData} written at the end of a
session, a knitr cache or a targets store may hold a connection without
writing a token to disk. A connection read back in another session has a
dead id and resolves its profile afresh, so it keeps working if that
profile is still stored.

This is a guarantee about this package's own objects. A token you assign
to a variable yourself, or pass as \verb{token =} and keep, is an ordinary
string and is saved like one.
}

\examples{
\dontrun{
# needs a JATOS server and a stored API token
conn <- jatos_connection()
conn
admin <- jatos_connection("lab_admin")
jatos_studies(conn = admin)
}
}
