% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sitrep.R
\name{jatos_credentials_sitrep}
\alias{jatos_credentials_sitrep}
\title{Report where each token is coming from}
\usage{
jatos_credentials_sitrep(profile = NULL)
}
\arguments{
\item{profile}{Report only this profile. \code{NULL}, the default, reports
every profile found in any of the three places.}
}
\value{
A tibble, invisibly, with one row per profile: \code{profile}, \code{host},
\code{host_from} (\code{"env"}, \code{"config"} or \code{"none"}), \code{auth_from} (\code{"env"},
\code{"keyring"} or \code{"none"}), \code{shadowed} (an environment variable is hiding a
stored token), \code{renviron_line} (an \code{.Renviron} file defines this
profile) and \code{renviron_file}. No column holds a token.
}
\description{
Prints, for every credential profile on this machine, which of the three
places the package reads a host and a token would actually be taken from:
an environment variable, the system credential store, or the profile
configuration file. It never prints a token, and never a hash, a prefix or
a length of one.
}
\details{
Use it when a call fails with credentials that look correct, and when two
profiles might be confused. The report starts with the versions of
jatosr, \code{keyring}, R and the operating system (and the commit, for a
package installed from GitHub), so that it can be pasted into a bug
report as it is, once the host is replaced if the server is private.

An environment variable wins over the credential store. When a user or
project \code{.Renviron} file defines the variables of a profile, the report
names the file and the lines, with the token's value hidden, because a
token stored with \code{\link[=jatos_set_credentials]{jatos_set_credentials()}} is not used while such a line
sets the variable. The file is only read, never edited.
}
\examples{
\dontrun{
# lists the machine's credential store
jatos_credentials_sitrep()
jatos_credentials_sitrep("lab_admin")
}
}
\seealso{
\code{\link[=jatos_list_profiles]{jatos_list_profiles()}}, \code{\link[=jatos_set_credentials]{jatos_set_credentials()}},
\code{vignette("credentials")}.
}
