% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import-results.R
\name{jatos_import_results}
\alias{jatos_import_results}
\title{Import a JATOS results zip into the cache}
\usage{
jatos_import_results(zip, path, overwrite = FALSE)
}
\arguments{
\item{zip}{Path of the zip file.}

\item{path}{The cache directory. Created when missing; must be empty or
in the package layout.}

\item{overwrite}{If \code{TRUE}, files of a batch that is already in the
cache are replaced.}
}
\value{
The metadata tibble of the imported batches as
\code{\link[=jatos_read_metadata]{jatos_read_metadata()}} returns it, invisibly; a message reports the
batches, component results and files imported.
}
\description{
Unpacks a zip that JATOS itself produced, from the GUI's "Export Results"
or from \code{\link[=jatos_export_archive]{jatos_export_archive()}}, into the cache layout that
\code{\link[=jatos_download_results]{jatos_download_results()}} writes: \code{metadata.json} at the zip root and
\verb{study_result_<id>/comp-result_<id>/data.txt} (plus \verb{files/<name>} for
uploads) become \verb{<path>/batch_<id>/metadata.json} and
\verb{<path>/batch_<id>/study_result_<id>/comp-result_<id>/...}, one batch
folder per batch id in the zip's \code{metadata.json}, each with a
\code{metadata.json} holding only that batch's study results. This is the
one way data that did not come through the package enters a cache; the
reader, \code{\link[=jatos_cache_status]{jatos_cache_status()}} and later incremental downloads then
treat it like any other cache. Entries whose study result the
\code{metadata.json} does not list are left out with a message.
}
\details{
A batch folder that already exists in \code{path} is refused unless
\code{overwrite = TRUE}, which replaces its \code{metadata.json} and every file the
zip carries and leaves other files in place.
}
\examples{
zip <- system.file("extdata", "results.zip", package = "jatosr")

cache <- tempfile("jatosr-example-")
jatos_import_results(zip, cache)
jatos_cache_status(cache)
unlink(cache, recursive = TRUE)
}
\seealso{
\code{\link[=jatos_export_archive]{jatos_export_archive()}} to fetch such a zip from the server,
\code{\link[=jatos_read_metadata]{jatos_read_metadata()}} and \code{\link[=jatos_export_results]{jatos_export_results()}} with
\code{download = FALSE} to work from the cache.
}
