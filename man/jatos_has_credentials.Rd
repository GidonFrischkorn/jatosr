% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile.R
\name{jatos_has_credentials}
\alias{jatos_has_credentials}
\title{Are JATOS credentials available?}
\usage{
jatos_has_credentials(profile = Sys.getenv("JATOS_PROFILE", "default"))
}
\arguments{
\item{profile}{Name of the credential profile, as given to
\code{\link[=jatos_set_credentials]{jatos_set_credentials()}}. Defaults to the \code{JATOS_PROFILE} environment
variable, or \code{"default"} when that is unset, so a single-account setup
never passes it. Case-insensitive.}
}
\value{
\code{TRUE} or \code{FALSE}.
}
\description{
Checks whether a host and a token can be found for a credential profile:
the host from \code{JATOS_HOST} (or \verb{JATOS_HOST_<PROFILE>}) or the profile
configuration file, the token from \code{JATOS_TOKEN} (or
\verb{JATOS_TOKEN_<PROFILE>}) or the system credential store. No secret is
retrieved and no request is made; use \code{\link[=jatos_token_info]{jatos_token_info()}} to find out
whether the token also works.
}
\examples{
\dontrun{
# lists the machine's credential store
jatos_has_credentials()
jatos_has_credentials("lab_admin")
}
}
