% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write-raw.R
\name{jatos_write_raw}
\alias{jatos_write_raw}
\title{Copy the raw result files, one per participant}
\usage{
jatos_write_raw(metadata, path, name_by = "study_result_id", overwrite = FALSE)
}
\arguments{
\item{metadata}{A metadata tibble with the \code{file} column, from
\code{\link[=jatos_download_results]{jatos_download_results()}} or \code{\link[=jatos_read_metadata]{jatos_read_metadata()}}. Rows without a
local file are left out with a message.}

\item{path}{Directory to write into; created when missing.}

\item{name_by}{Name of the metadata column whose value names each file.
\code{NA} values are refused, and so is a value that could leave \code{path}:
one holding a slash or a backslash, or equal to \code{.} or \code{..}. The
values may come from the participants themselves (a Prolific id in
the URL, a field of the result data), so they are never trusted as
paths.}

\item{overwrite}{If \code{TRUE}, existing files in \code{path} are replaced.}
}
\value{
A tibble with \code{study_result_id}, \code{component_result_id}, \code{file}
(the source) and \code{path} (the copy), invisibly; a message reports the
count.
}
\description{
Copies every local \code{data.txt} of a metadata tibble byte for byte to
\verb{<path>/<name>.json}, named by a column of the metadata (the study result
id by default, or \code{worker_id}, \code{query_prolific_pid}, or a field from
\code{\link[=jatos_extract_fields]{jatos_extract_fields()}}), which is the layout labs share raw JSON in.
Nothing is parsed or rewritten. A study result with several files (one
per component) gets \verb{<name>_<component_result_id>.json} for each. When
two different study results share a name, for example a participant who
ran twice, nothing is written and the error lists the name and the study
result ids, so that duplicates are resolved on purpose rather than
silently. Each copy is written under a temporary name in \code{path} and
renamed into place, so a copy that fails never sits under the real
name.
}
\examples{
cache <- system.file("extdata", "JATOS_data", package = "jatosr")
meta <- jatos_read_metadata(cache)

out <- tempfile("jatosr-example-")
jatos_write_raw(meta, out, name_by = "worker_id")
list.files(out)
unlink(out, recursive = TRUE)
}
\seealso{
\code{\link[=jatos_export_archive]{jatos_export_archive()}} for the server's own zip of the same
files.
}
