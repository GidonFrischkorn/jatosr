% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/study-results.R
\name{jatos_study_results}
\alias{jatos_study_results}
\title{One row per study result}
\usage{
jatos_study_results(metadata, participant = NULL)
}
\arguments{
\item{metadata}{A metadata tibble from \code{\link[=jatos_results_metadata]{jatos_results_metadata()}},
\code{\link[=jatos_read_metadata]{jatos_read_metadata()}} or \code{\link[=jatos_flatten_metadata]{jatos_flatten_metadata()}}.}

\item{participant}{Name of the column that identifies a participant,
for \code{n_runs}; \code{NULL} uses \code{query_prolific_pid} when present.}
}
\value{
A tibble with the study and study-result columns of \code{metadata},
plus \code{n_component_results}, \code{n_components} (distinct component ids),
\code{n_finished} (component results in state \code{FINISHED}), \code{data_size}
(bytes, summed), \code{n_files}, \code{first_component_start},
\code{last_component_end}, \code{n_runs} (when a participant key is available;
\code{NA} where the key is \code{NA}), any extra columns of \code{metadata}, and
\code{conflicts} (list of column names; only present when \code{metadata} has
extra columns).
}
\description{
Collapses a metadata tibble (one row per component result) to one row
per study result, which is the unit that means "one run by one
participant". Counts of people, duplicate detection and quality flags
belong at this level; a participant who reloaded a component has several
component results but one study result.
}
\details{
Columns that are not part of the metadata contract (for example
\code{participant_id} added by \code{jatos_extract_fields()}, or a column you
joined yourself) are carried over with their first non-\code{NA} value per
study result. When the component results of one study result disagree
on such a column, the value becomes \code{NA}, the column name is listed in
\code{conflicts}, and a warning names the study results concerned. The
\verb{<field>_status} columns of \code{\link[=jatos_extract_fields]{jatos_extract_fields()}} (recorded in the
\code{jatosr_extracted} attribute) and the local \code{file}, \code{file_size} and
\code{status} columns are left out.

Prolific returners and reloaded links produce several study results per
person. When a participant key is available, \code{n_runs} counts the study
results per key value, so duplicates are visible before N is counted:
\code{participant} names the column (a \verb{query_*} column from
\code{\link[=jatos_url_query]{jatos_url_query()}}, a field from \code{\link[=jatos_extract_fields]{jatos_extract_fields()}}, or
\code{worker_id}); with \code{participant = NULL} the column \code{query_prolific_pid}
is used when present, and \code{n_runs} is left out otherwise. A message
reports how many keys have more than one run.
}
\examples{
meta <- jatos_flatten_metadata(
  system.file("extdata", "metadata.json", package = "jatosr")
)
jatos_study_results(meta)[, c("study_result_id", "n_component_results", "data_size")]
jatos_study_results(meta, participant = "worker_id")[, c("study_result_id", "worker_id", "n_runs")]
}
