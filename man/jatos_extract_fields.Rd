% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-fields.R
\name{jatos_extract_fields}
\alias{jatos_extract_fields}
\title{Extract scalar fields from result files without parsing them}
\usage{
jatos_extract_fields(metadata, fields, warn = TRUE, file_col = "file")
}
\arguments{
\item{metadata}{A metadata tibble with the local file column (from
\code{\link[=jatos_download_results]{jatos_download_results()}} or \code{\link[=jatos_read_metadata]{jatos_read_metadata()}}).}

\item{fields}{Character vector of key names to extract.}

\item{warn}{If \code{FALSE}, the closing warnings (\code{conflict} and \code{unparseable}
files, disagreements with the parser) are not raised; the status
columns still say it all. Files without the key (\code{absent}) are reported
in a message either way, since a key that only some components carry
is not a fault.}

\item{file_col}{Name of the column holding the local file paths.}
}
\value{
\code{metadata} with two columns per field: \verb{<field>} and
\verb{<field>_status}, and the attribute \code{jatosr_extracted} naming every
extracted field. The value column is numeric when every value is a JSON
number, logical when every value is \code{true} or \code{false}, character
otherwise; JSON \code{null} is \code{NA}. The status is \code{unique} (all occurrences
agree), \code{absent} (no occurrence), \code{conflict} (occurrences disagree, or an
occurrence holds an array or object), or \code{unparseable} (the file has no
unique value and the parser could not read it). Rows without a local
file get \code{NA} in both columns.
}
\description{
Reads one or more JSON keys (for example \code{participant_id}) out of every
downloaded \code{data.txt} by regular expression, which is several times faster
than parsing each file and works on files that are not valid JSON as a
whole. The pattern is anchored on the key position (\verb{"field":}), so the
same text inside a value, say a URL query string, is not matched. All
key-position occurrences are collected, at any depth of the JSON: when
they agree the value is returned; when they disagree the value is \code{NA}
and the row is flagged.
}
\details{
Every file where the regular expression found disagreeing values (status
\code{conflict}) is then parsed in full with the same parser
\code{\link[=jatos_read_json]{jatos_read_json()}} uses, every value stored under the key at any depth
is collected from the parsed objects, and the two readings are compared;
so is every file without the key (status \code{absent}) as long as the key was
found in some other file, since a key present in one file and missing
in the next may be an escaped spelling the regular expression does not
see. On a file with one agreeing value the regular expression cannot be
wrong, so those files are not parsed, and a key found in no file at all
is reported \code{absent} everywhere without parsing (read one file with
\code{\link[=jatos_read_json]{jatos_read_json()}} when that is a surprise). A disagreement means the
regular expression read a file differently from the parser (an escaped
key, an unusual encoding) and is a warning naming the files; a message
reports the tally. A file the parser refuses gets status \code{unparseable},
which means exactly that \code{\link[=jatos_read_results]{jatos_read_results()}} would stop on it.

A field whose name is already a column of \code{metadata} (\code{batch_id},
\code{worker_id}, \code{file}, ...) is refused before any file is read, since the
extracted values would silently replace the server's. The two columns
of a previous extraction of the same field are replaced; which columns
came from an extraction is recorded in the attribute
\code{jatosr_extracted} of the returned tibble, so a column pair of your own
that happens to end in \verb{_status} is never mistaken for one.
}
\examples{
cache <- system.file("extdata", "JATOS_data", package = "jatosr")
meta <- jatos_read_metadata(cache)
meta <- jatos_extract_fields(meta, c("participant_id", "age"))
meta[, c("study_result_id", "participant_id", "participant_id_status", "age")]
}
\seealso{
\code{\link[=jatos_study_results]{jatos_study_results()}}, which carries the value columns to the
study-result level and flags component results that disagree.
}
