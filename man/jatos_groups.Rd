% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/studies.R
\name{jatos_groups}
\alias{jatos_groups}
\title{Groups of a batch (group studies only)}
\usage{
jatos_groups(batch_id, conn = jatos_connection())
}
\arguments{
\item{batch_id}{Batch id (integer) or uuid (string).}

\item{conn}{A \code{\link[=jatos_connection]{jatos_connection()}}.}
}
\value{
A tibble with \code{group_id}, \code{group_state}, \code{n_active}, \code{n_history},
\code{n_results}, \code{active_members}, \code{history_members} (list columns of study
result ids), \code{start_time}, \code{end_time}, \code{batch_id}.
}
\description{
Calls \code{GET /batches/{id}/groups}.
}
\section{Server support}{

Like \code{\link[=jatos_batches]{jatos_batches()}}, this is a nested route under a resource, and the
one server where those were tested (\code{apiVersion} 1.0.1) has none of
them. It was not confirmed for this endpoint — no group study was
available there — so treat a 404 as a possible missing route rather than
a wrong batch id; the error distinguishes the two from the server's own
wording.
}

\examples{
\dontrun{
# needs a JATOS server and a stored API token
jatos_groups(34)
}
}
