% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-results.R
\name{jatos_export_results}
\alias{jatos_export_results}
\title{Fetch, read and save the trials of a study in one call}
\usage{
jatos_export_results(
  study_id = NULL,
  batch_id = NULL,
  ...,
  cache,
  file,
  download = TRUE,
  fields = NULL,
  states = NULL,
  worker_types = NULL,
  since = NULL,
  until = NULL,
  exclude_study_result_id = NULL,
  exclude_worker_id = NULL,
  tz = "UTC",
  reader = NULL,
  coerce = c("error", "character"),
  on_error = c("abort", "skip"),
  metadata_cols = default_metadata_cols(),
  format = NULL,
  split = c("none", "component"),
  metadata_file = TRUE,
  provenance = TRUE,
  archive_study = FALSE,
  archive_results = FALSE,
  overwrite = FALSE,
  conn = jatos_connection()
)
}
\arguments{
\item{study_id, batch_id}{One or more ids to select results by; at least
one of the two must be given for a download. Passed to
\code{\link[=jatos_results_metadata]{jatos_results_metadata()}}; \code{study_id} may also be one or more uuid
strings. With \code{download = FALSE} both may be \code{NULL}, which exports the
whole cache.}

\item{...}{Must be empty.}

\item{cache}{The local cache directory, see \code{\link[=jatos_download_results]{jatos_download_results()}}.}

\item{file}{Path of the trials file to write, see \code{\link[=jatos_write_results]{jatos_write_results()}}.
Its directory must exist.}

\item{download}{If \code{FALSE}, nothing is requested: the metadata is read
from the cache and the dataset is built from the files already there.}

\item{fields}{JSON keys to extract with \code{\link[=jatos_extract_fields]{jatos_extract_fields()}} and
join to every trial row. For values that do not sit at the top level
of the trial objects, for example inside a survey response; a field
that already is a trial column is reported as a clash by
\code{\link[=jatos_read_results]{jatos_read_results()}}.}

\item{states}{Study states to keep, for example \code{"FINISHED"}. \code{NULL}
keeps every state. JATOS states are \code{PRE}, \code{STARTED}, \code{DATA_RETRIEVED},
\code{FINISHED}, \code{ABORTED} and \code{FAIL}.}

\item{worker_types}{Worker types to keep, for example
\code{c("PersonalSingle", "GeneralMultiple")}. \code{NULL} keeps every type; the
GUI test runs are \code{"Jatos"}.}

\item{since}{Keep study results that started at or after this time: a
\code{POSIXct}, a \code{Date}, or a string such as \code{"2025-08-24"} or
\code{"2025-08-24 12:00:00"}, read in the time zone \code{tz}. Rows without a
start time are dropped when \code{since} is given.}

\item{until}{Keep study results that started \emph{before} this time, given
like \code{since}; \code{since} and \code{until} together select a half-open
interval, so \verb{since = "2025-08-24", until = "2025-08-25"} is one day.
Rows without a start time are dropped when \code{until} is given.}

\item{exclude_study_result_id, exclude_worker_id}{Study result ids, or
worker ids, whose study results are dropped; for the researcher's own
runs through a real link, which \code{worker_types} cannot tell apart.}

\item{tz}{Time zone in which date strings in \code{since} and \code{until} are
read, and in which the message shows them. \code{"UTC"} by default, the
zone the server reports times in; \code{Sys.timezone()} for local time.}

\item{reader}{\code{NULL} for \code{\link[=jatos_read_json]{jatos_read_json()}}, or a function of one file
path returning a data frame with one row per trial. PsychoJS, for
instance, writes csv: \code{reader = function(file) utils::read.csv(file)}.}

\item{coerce}{What to do with a column whose atomic type differs between
files: \code{"error"} stops with the column named; \code{"character"} converts
that column to character in every file and reports it.}

\item{on_error}{\code{"abort"} stops at the first file the reader cannot read.
\code{"skip"} leaves such files out, reads the rest, and warns once with
the files and the first error; a clash between trial and metadata
columns still aborts.}

\item{metadata_cols}{Metadata columns joined to every trial row after
the ids, see \code{\link[=jatos_read_results]{jatos_read_results()}}; the \verb{query_*} columns and the
extracted \code{fields} are added to whatever is given here. \code{NULL} joins
only those.}

\item{format}{Format of the trials file, passed to
\code{\link[=jatos_write_results]{jatos_write_results()}}; \code{NULL} infers it from the extension of
\code{file}. The default metadata file shares it; a metadata file given as
a path takes its format from its own extension.}

\item{split}{\code{"none"} writes one trials file. \code{"component"} reads and
writes one file per component id (\verb{<stem>_component_<id>.<ext>}), see
\code{\link[=jatos_read_results]{jatos_read_results()}}; the metadata file is written once.}

\item{metadata_file}{\code{TRUE} writes the study-result table next to \code{file}
as \verb{<stem>_metadata.<ext>}; a path writes it there, in the format of
that path's extension (a \code{.csv} next to an \code{.rds} trials file is
fine); \code{FALSE} skips it.}

\item{provenance}{If \code{TRUE}, write \verb{<stem>_export.json} with the host,
profile, ids, filters, fields, cache, package version, time, counts and
the paths written.}

\item{archive_study}{If \code{TRUE}, the study archive is downloaded with
\code{\link[=jatos_export_study]{jatos_export_study()}} and written next to the dataset: as
\verb{<stem>_study.jzip} for the one study named in \code{study_id}, as
\verb{<stem>_study_<id>.jzip} per study when several are named or when only
\code{batch_id} is given and the studies come from the metadata. The
archive records the experiment that produced the data; the provenance
record lists it.}

\item{archive_results}{If \code{TRUE}, the results archive that the server
builds for the same ids (\code{POST /results}: every \code{data.txt}, the
uploaded files and the \code{metadata.json}, see \code{\link[=jatos_export_archive]{jatos_export_archive()}})
is written next to the dataset as \verb{<stem>_results.zip}, and the
provenance record lists it with its size and md5 checksum. An
archival copy, fetched in full each time; the incremental download
into \code{cache} is unchanged.}

\item{overwrite}{Passed to \code{\link[=jatos_write_results]{jatos_write_results()}} for every file
written. The download keeps its own guard for local files larger than
the server's copy.}

\item{conn}{A \code{\link[=jatos_connection]{jatos_connection()}}; not used with \code{download = FALSE}.}
}
\value{
The trials tibble, invisibly (with \code{split = "component"} a named
list of tibbles); a message reports the row count and the paths
written.
}
\description{
Runs the whole pipeline: \code{\link[=jatos_results_metadata]{jatos_results_metadata()}} for the ids given,
\code{\link[=jatos_filter_metadata]{jatos_filter_metadata()}} with \code{states}, \code{worker_types}, \code{since},
\code{until} and the two exclusion lists,
\code{\link[=jatos_download_results]{jatos_download_results()}} into \code{cache}, \code{\link[=jatos_url_query]{jatos_url_query()}},
\code{\link[=jatos_extract_fields]{jatos_extract_fields()}} when \code{fields} is given, \code{\link[=jatos_read_results]{jatos_read_results()}}
with the metadata joined, and \code{\link[=jatos_write_results]{jatos_write_results()}} to \code{file}. Nothing
happens here that those functions do not do on their own; run them
separately when a step needs an argument this wrapper does not pass on
(\code{chunk_size} of the download, \code{flatten} of the reader). \code{reader},
\code{split}, \code{coerce} and \code{on_error} go to the reader as they are; with
\code{on_error = "skip"} a file the reader cannot read is left out of the
trials with a warning, and the final message and the provenance count
only the files that were read. Files that
participants uploaded are not part of a trials dataset; fetch them with
\code{\link[=jatos_download_files]{jatos_download_files()}}.
}
\details{
With \code{download = FALSE} the same dataset is built from the cache alone:
the metadata comes from the cache's \code{metadata.json} files through
\code{\link[=jatos_read_metadata]{jatos_read_metadata()}} (restricted to \code{study_id} and \code{batch_id} when
given, the whole cache otherwise), no request is made and no connection
is needed, and the provenance record says \code{offline: true} and lists the
metadata files with their modification times. That is the path for an
analysis machine without credentials, for CI, or for rebuilding a
dataset after the study left the server; rows whose data file is not in
the cache are skipped with a message, as \code{\link[=jatos_read_results]{jatos_read_results()}} does.
\code{archive_study} needs the server and is refused offline.

Three files come out of one call, one more each with \code{archive_study = TRUE} and \code{archive_results = TRUE}. The trials file holds one row per
trial with the ids, \code{batch_id}, \code{component_id}, \code{worker_id},
\code{worker_type}, \code{study_state}, \code{study_start_time}, the URL query
parameters as \verb{query_*} columns and the extracted \code{fields} in front of
the trial columns. The metadata file (\verb{<stem>_metadata.<ext>} by default)
holds one row per study result from \code{\link[=jatos_study_results]{jatos_study_results()}},
which is where exclusions, payments and the participant count of a
methods section come from; it covers the same study results as the
trials file. The provenance file (\verb{<stem>_export.json}) records host,
ids, filters, package version, time and the counts of the export.

Run it again later and only new or grown results are downloaded; the
files are then rewritten from the whole cache, which is why \code{overwrite}
exists. \code{file}, \code{format}, the target directory, the sidecar paths and
\code{fields} are checked before the first request, so a mistake there costs
no download; every path the call will write (with \code{split = "component"}
the part files, whose names come from the component ids of the
metadata) is checked once more right after the metadata answer, before
the download. A component result whose download failed, or that the
server's answer did not contain, stops the export before anything is
written, since the dataset would be incomplete; the files fetched so far
stay in the cache for the next run.
}
\examples{
\dontrun{
# needs a JATOS server and a stored API token
trials <- jatos_export_results(
  study_id = 12, cache = "JATOS_data", file = "data/study12.rds",
  states = "FINISHED"
)

# one batch, as csv, without the researcher's own GUI runs, with a field
# that sits inside a survey response
jatos_export_results(
  batch_id = 34, cache = "JATOS_data", file = "data/batch34.csv",
  worker_types = c("PersonalSingle", "GeneralMultiple"),
  fields = "age", overwrite = TRUE
)

# the same dataset from the cache alone, without credentials
jatos_export_results(
  study_id = 12, cache = "JATOS_data", file = "data/study12.rds",
  states = "FINISHED", download = FALSE, overwrite = TRUE
)
}
}
\seealso{
\code{\link[=jatos_filter_metadata]{jatos_filter_metadata()}}, \code{\link[=jatos_url_query]{jatos_url_query()}},
\code{\link[=jatos_study_results]{jatos_study_results()}} for the pieces this adds to the five steps.
}
