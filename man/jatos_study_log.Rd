% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/studies.R
\name{jatos_study_log}
\alias{jatos_study_log}
\title{Recent entries of a study log}
\usage{
jatos_study_log(study_id, limit = 100, conn = jatos_connection())
}
\arguments{
\item{study_id}{Study id (integer) or uuid (string).}

\item{limit}{Maximum number of most recent log lines to return.}

\item{conn}{A \code{\link[=jatos_connection]{jatos_connection()}}.}
}
\value{
A tibble with one row per log entry and a \code{study_id} column.
Nested values are kept as list columns.
}
\description{
Calls \verb{GET /studies/\{id\}/log?download=false&entryLimit=}. JATOS streams
the log as newline-delimited JSON; each line becomes one row. Column
names follow the log entry fields (converted to snake case), which vary
between JATOS versions and entry types, so downstream code should select
columns by name defensively.
}
\examples{
\dontrun{
# needs a JATOS server and a stored API token
jatos_study_log(12, limit = 20)
}
}
