% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/study-codes.R
\name{jatos_study_links}
\alias{jatos_study_links}
\title{Run URLs for study codes}
\usage{
jatos_study_links(
  code,
  host = NULL,
  profile = Sys.getenv("JATOS_PROFILE", "default")
)
}
\arguments{
\item{code}{One or more study codes.}

\item{host}{Base URL of the JATOS server. Defaults to the host variable of
the credential profile (\code{JATOS_HOST} for the default profile); the token
is not needed.}

\item{profile}{Name of the credential profile, as given to
\code{\link[=jatos_set_credentials]{jatos_set_credentials()}}. Defaults to the \code{JATOS_PROFILE} environment
variable, or \code{"default"} when that is unset, so a single-account setup
never passes it. Case-insensitive.}
}
\value{
A character vector of URLs, one per code.
}
\description{
Turns study codes into the links participants open, \verb{<host>/publix/<code>}.
No request is made. The pattern is the \verb{GET /publix/:studyCode} route of
the JATOS publix module (\code{modules/publix/conf/publix.routes} in the JATOS
repository, and the \code{studyLinkPath} field of \code{GET /studyCodes/{code}} in
\code{jatos-api.yaml}, both read 2026-09-05). Any base path under which JATOS
is served is part of \code{host}.
}
\examples{
jatos_study_links(c("8kw0pFV5M1e", "Qm3xYtb9Lc2"), host = "https://jatos.example.org")
}
