% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/token.R
\name{jatos_token_info}
\alias{jatos_token_info}
\title{Check the token and show its metadata}
\usage{
jatos_token_info(conn = jatos_connection())
}
\arguments{
\item{conn}{A \code{\link[=jatos_connection]{jatos_connection()}}.}
}
\value{
A one-row tibble with \code{token_id}, \code{name}, \code{username}, \code{user_id},
\code{created}, \code{expires} (both POSIXct, UTC; \code{expires} is \code{NA} for tokens
without expiry), \code{expired}, \code{active}, and \code{roles} (list column).
}
\description{
Calls \code{GET /admin/token}, which any token may use to inspect itself. This
is the cheapest way to verify that the host and token of a profile work:
a wrong token gives a 401 error, a wrong host a 404 or an HTML page.
}
\examples{
\dontrun{
# needs a JATOS server and a stored API token
jatos_token_info()
}
}
