% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-results.R
\name{jatos_read_results}
\alias{jatos_read_results}
\title{Read the result files of a metadata tibble into one tibble}
\usage{
jatos_read_results(
  metadata,
  ...,
  flatten = FALSE,
  id_cols = c("study_result_id", "component_result_id"),
  metadata_cols = default_metadata_cols(),
  reader = NULL,
  on_error = c("abort", "skip"),
  split = c("none", "component"),
  coerce = c("error", "character")
)
}
\arguments{
\item{metadata}{A metadata tibble with a \code{file} column (from
\code{\link[=jatos_download_results]{jatos_download_results()}} or \code{\link[=jatos_read_metadata]{jatos_read_metadata()}}), or a character
vector of file paths. Of a metadata tibble only \code{file} and the columns
named in \code{id_cols}, \code{metadata_cols} and, for the split, \code{component_id}
are used.}

\item{...}{Must be empty.}

\item{flatten}{Passed to \code{\link[=jatos_read_json]{jatos_read_json()}}; ignored with a \code{reader}.}

\item{id_cols}{Columns of \code{metadata} to prepend to each file's trials.
Ignored when \code{metadata} is a character vector; the file path is then
prepended as \code{file}.}

\item{metadata_cols}{Further columns of \code{metadata} to join after
\code{id_cols}, for example a field added by \code{\link[=jatos_extract_fields]{jatos_extract_fields()}} that is
not a trial column. \code{NULL} joins the id columns only. Ignored when
\code{metadata} is a character vector.}

\item{reader}{\code{NULL} for \code{\link[=jatos_read_json]{jatos_read_json()}}, or a function of one file
path returning a data frame with one row per trial.}

\item{on_error}{\code{"abort"} stops at the first file the reader cannot read.
\code{"skip"} leaves such files out, reads the rest, and warns once with
the files and the first error; a clash between trial and metadata
columns still aborts.}

\item{split}{\code{"none"} returns one tibble. \code{"component"} returns a named
list with one tibble per distinct \code{component_id} of \code{metadata}, in the
order the ids first appear; needs a metadata tibble.}

\item{coerce}{What to do with a column whose atomic type differs between
files: \code{"error"} stops with the column named; \code{"character"} converts
that column to character in every file and reports it.}
}
\value{
A tibble with one row per trial, or with \code{split = "component"}
a named list of such tibbles. Rows of \code{metadata} without a local file
are left out with a message.
}
\description{
Reads every local \code{data.txt} listed in \code{metadata} with
\code{\link[=jatos_read_json]{jatos_read_json()}} (or with \code{reader}) and binds the trials of all files,
filling columns that some files lack with \code{NA}. Columns of the metadata
are joined to each file's trials, so that every row knows which study
result and component result it came from and carries the batch,
component, worker and study state of that run: first \code{id_cols}, then
\code{metadata_cols}, then the trial columns. The join costs nothing to look
up, since one metadata row is one file.
}
\details{
A nested object in the trials (a survey \code{response}, say) comes out of
\code{jsonlite} as a data-frame column in a file where every trial carries an
object with the same keys, and as a list column in a file where the
objects differ, so two files of the same component can disagree on the
column's type. Before binding, every nested data-frame column is
therefore stored as a list column, one list per trial with the keys that
trial had (\code{NULL} where the trial had no object); a message names the
columns. A column whose \emph{atomic} type differs between files (a number
in one file, a string in another) is an error naming the column; with
\code{coerce = "character"} such columns are converted to character in every
file, with a message, and the bind goes on.

A trial column that has the same name as a joined metadata column is an
error naming the column and the file; nothing is renamed or suffixed.
That happens, for example, when the experiment stored \code{worker_id} in the
data itself, or when a field extracted with \code{\link[=jatos_extract_fields]{jatos_extract_fields()}} is
named in \code{metadata_cols} although it is a top-level field of every
trial already (the usual case for a \code{participant_id} set with
\code{jsPsych.data.addProperties()}). Leave such a column out of
\code{metadata_cols}; the trials carry it.

Result files that are not jsPsych JSON arrays (PsychoJS csv, OSWeb or
lab.js output) are read with a \code{reader} of your own, a function that
takes one file path and returns a data frame; the join and the binding
are the same. A study whose components write different columns is read
with \code{split = "component"}, one tibble per component, which also keeps a
column that changes type between components from blocking the bind.
}
\examples{
cache <- system.file("extdata", "JATOS_data", package = "jatosr")
meta <- jatos_read_metadata(cache)
trials <- jatos_read_results(meta[meta$component_state == "FINISHED", ])

# a field that sits inside a survey response, not at the trial level
meta <- jatos_extract_fields(meta, "age")
trials <- jatos_read_results(meta, metadata_cols = c("worker_id", "age"))
trials[, c("study_result_id", "worker_id", "age", "trial_index", "rt")]

# one tibble per component
parts <- jatos_read_results(meta, split = "component")
names(parts)

# PsychoJS writes csv, read with a reader of your own:
# trials <- jatos_read_results(meta, reader = function(file) utils::read.csv(file))
}
\seealso{
\code{\link[=jatos_write_results]{jatos_write_results()}} to save the tibble,
\code{\link[=jatos_export_results]{jatos_export_results()}} for the whole pipeline in one call.
}
