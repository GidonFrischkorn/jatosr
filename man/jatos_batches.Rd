% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/studies.R
\name{jatos_batches}
\alias{jatos_batches}
\title{Batches of a study}
\usage{
jatos_batches(study_id, conn = jatos_connection())
}
\arguments{
\item{study_id}{Study id (integer) or uuid (string).}

\item{conn}{A \code{\link[=jatos_connection]{jatos_connection()}}.}
}
\value{
A tibble with \code{batch_id}, \code{batch_uuid}, \code{title}, \code{active},
\code{max_total_workers}, \code{max_active_members}, \code{max_total_members},
\code{allowed_worker_types} (list column), \code{study_id}.
}
\description{
Calls \code{GET /studies/{id}/batches}.
}
\section{Server support}{

This endpoint does not exist on every JATOS. On a server whose API
reports \code{apiVersion} 1.0.1 it answers 404 for a study that
\code{\link[=jatos_studies]{jatos_studies()}} has just listed, and the error says so. There is no
version endpoint to check in advance; \code{apiVersion} in the envelope of
any successful answer is the only signal.

\code{jatos_studies(with_batches = TRUE)} (the default) is the way round it,
and is cheaper anyway: it returns every study's batches as a nested
\code{batches} column in one request rather than one request per study.
}

\examples{
\dontrun{
# needs a JATOS server and a stored API token
jatos_batches(12)
}
}
