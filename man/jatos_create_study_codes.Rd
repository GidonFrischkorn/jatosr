% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/study-codes.R
\name{jatos_create_study_codes}
\alias{jatos_create_study_codes}
\title{Generate study codes for a batch}
\usage{
jatos_create_study_codes(
  study_id,
  batch_id = NULL,
  n = 1,
  type = c("PersonalSingle", "PersonalMultiple", "GeneralSingle", "GeneralMultiple",
    "MTurk"),
  comment = NULL,
  conn = jatos_connection()
)
}
\arguments{
\item{study_id}{Study id (integer) or uuid (string).}

\item{batch_id}{Batch id. \code{NULL} (the default) uses the study's default
batch; the returned \code{batch_id} is then \code{NA} because the server does not
report which batch that is.}

\item{n}{Number of codes to generate, \code{1} to \code{1000} (the server's limit);
personal types only.}

\item{type}{Worker type of the codes. One of \code{"PersonalSingle"},
\code{"PersonalMultiple"}, \code{"GeneralSingle"}, \code{"GeneralMultiple"}, \code{"MTurk"}.}

\item{comment}{Optional comment stored with each worker; personal types
only. Plain text up to 255 characters, no HTML.}

\item{conn}{A \code{\link[=jatos_connection]{jatos_connection()}}.}
}
\value{
A tibble with one row per code: \code{study_code}, \code{study_id} (\code{NA}
when \code{study_id} was a uuid), \code{batch_id}, \code{type}, \code{comment}, and
\code{study_link}, the run URL built by \code{\link[=jatos_study_links]{jatos_study_links()}}.
}
\description{
Calls \code{POST /studies/{id}/studyCodes}. A study code is what JATOS puts at
the end of a study link (\verb{<host>/publix/<code>}); each code belongs to one
batch and one worker type. For the personal types (\code{PersonalSingle},
\code{PersonalMultiple}) the server creates \code{n} new codes, each with its own
worker and the optional \code{comment}. For the general types (\code{GeneralSingle},
\code{GeneralMultiple}, \code{MTurk}) a batch has exactly one code, which the server
creates on first request and returns unchanged afterwards; \code{n} must be \code{1}
and \code{comment} \code{NULL} for them, checked before any request is made.
}
\details{
The server answers with the codes only (verified against \code{jatos-api.yaml},
2026-09-05). Whether a code is active, and its batch when \code{batch_id} was
not given, come from \code{\link[=jatos_study_code]{jatos_study_code()}}.
}
\examples{
\dontrun{
# needs a JATOS server and a stored API token
codes <- jatos_create_study_codes(12, batch_id = 34, n = 20, comment = "wave 2")
writeLines(codes$study_link)
}
}
\seealso{
\code{\link[=jatos_study_code]{jatos_study_code()}} for the properties of a code,
\code{\link[=jatos_deactivate_study_code]{jatos_deactivate_study_code()}} to close one, \code{\link[=jatos_study_links]{jatos_study_links()}} for
the run URLs.
}
