% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/study-codes.R
\name{jatos_study_code}
\alias{jatos_study_code}
\title{Properties of study codes}
\usage{
jatos_study_code(code, conn = jatos_connection())
}
\arguments{
\item{code}{One or more study codes, as returned by
\code{\link[=jatos_create_study_codes]{jatos_create_study_codes()}} or shown in the JATOS GUI.}

\item{conn}{A \code{\link[=jatos_connection]{jatos_connection()}}.}
}
\value{
A tibble with one row per code: \code{study_code}, \code{batch_id}, \code{type},
\code{comment}, \code{active}, \code{study_link} (\verb{<origin>/publix/<code>}) and
\code{study_entry_link} (\verb{<origin>/publix/run?code=<code>}, the form JATOS
uses for its own study entry page).
}
\description{
Calls \code{GET /studyCodes/{code}} once per code. The server reports the link
paths relative to its base path (\code{studyLinkPath}, \code{studyEntryPath} in
\code{jatos-api.yaml}); they are joined to the origin of \code{conn$host} to give
full URLs.
}
\examples{
\dontrun{
# needs a JATOS server and a stored API token
jatos_study_code("8kw0pFV5M1e")
}
}
