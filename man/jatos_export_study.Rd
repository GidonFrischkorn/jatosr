% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-study.R
\name{jatos_export_study}
\alias{jatos_export_study}
\title{Download the study archive}
\usage{
jatos_export_study(
  study_id,
  file = NULL,
  overwrite = FALSE,
  conn = jatos_connection()
)
}
\arguments{
\item{study_id}{Study id (integer) or uuid (string).}

\item{file}{Path to write, conventionally with the extension \code{.jzip}.
Its directory must exist. \code{NULL} takes the server's file name in the
working directory.}

\item{overwrite}{If \code{TRUE}, an existing file is replaced.}

\item{conn}{A \code{\link[=jatos_connection]{jatos_connection()}}.}
}
\value{
The path written, invisibly; a message reports it with the size.
}
\description{
Fetches the JATOS study archive of one study through \code{GET /studies/{id}}
and writes it to \code{file}. The archive is a zip that holds the study's
properties and components as JSON (a \code{.jas} file) next to the study
assets folder with the experiment's HTML, scripts and stimuli; JATOS
imports it as a \code{.jzip} file. Saved next to a dataset, it records the
exact version of the experiment that produced the data.
}
\details{
The archive is written under a temporary name in the target directory
and renamed into place after the answer was checked to be a zip file; an
existing \code{file} is never replaced unless \code{overwrite = TRUE}. Without
\code{file}, the archive lands in the working directory under the name the
server sends (\verb{jatos_study_<uuid>.jzip}, what a browser would save from
the GUI's export), reduced to its base name; \verb{jatos_study_<id>.jzip}
when the server names none. A file in the way is then found only after
the answer, so a run that is refused has already downloaded the archive
once. The token needs the user role for this endpoint, not only the
viewer role.
}
\examples{
\dontrun{
# needs a JATOS server and a stored API token
jatos_export_study(12, "data/study12_study.jzip")
jatos_export_study(12) # the server's name, here
}
}
\seealso{
\code{\link[=jatos_export_results]{jatos_export_results()}}, whose \code{archive_study = TRUE} writes
the archive next to the dataset.
}
