% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/results-metadata.R
\name{jatos_flatten_metadata}
\alias{jatos_flatten_metadata}
\title{Flatten a \verb{/results/metadata} answer into a tibble}
\usage{
jatos_flatten_metadata(metadata)
}
\arguments{
\item{metadata}{The metadata as the server sent it, not yet a tibble: a
parsed answer (the whole envelope or its \code{data} element) as returned by
\code{jsonlite::read_json(simplifyVector = FALSE)}, or the path of a
\code{metadata.json} file.}
}
\value{
A tibble with, per study: \code{study_id}, \code{study_uuid}, \code{study_title};
per study result: \code{study_result_id}, \code{study_result_uuid}, \code{study_code},
\code{worker_id}, \code{worker_type}, \code{batch_id}, \code{batch_uuid}, \code{batch_title},
\code{group_id}, \code{study_state}, \code{study_start_time}, \code{study_end_time},
\code{study_duration} (minutes), \code{last_seen}, \code{comment}, \code{study_message}
(the text shown when a run was aborted or failed), \code{confirmation_code}
(MTurk), \code{quota_reached}, \code{url_query} (list of the URL query parameters
the worker arrived with); per component
result: \code{component_result_id}, \code{component_id}, \code{component_uuid},
\code{component_state}, \code{component_start_time}, \code{component_end_time},
\code{component_duration} (minutes), \code{path} (location inside a result zip),
\code{data_size} (bytes on the server; 0 for reloads and unfinished runs),
\code{data_file}, \code{n_files} and \code{files} (list of attached-file entries).
}
\description{
Turns the nested JSON of \code{POST /results/metadata} (studies, their study
results, their component results) into a flat tibble with one row per
component result. Study-level and study-result-level values repeat down
their component results; every column name says which level it belongs
to, so \code{length(unique(meta$study_result_id))} counts participants while
\code{nrow(meta)} counts component results. Column names are snake case,
times are \code{POSIXct} in UTC, sizes are bytes.
}
\examples{
meta <- jatos_flatten_metadata(
  system.file("extdata", "metadata.json", package = "jatosr")
)
meta[, c("study_result_id", "component_result_id", "component_state", "data_size")]
}
