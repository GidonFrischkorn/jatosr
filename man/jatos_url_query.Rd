% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata-filter.R
\name{jatos_url_query}
\alias{jatos_url_query}
\title{Widen the URL query parameters into columns}
\usage{
jatos_url_query(metadata, prefix = "query_")
}
\arguments{
\item{metadata}{A metadata tibble with a \code{url_query} list column.}

\item{prefix}{Prefix of the new column names. The default keeps
\code{STUDY_ID} from colliding with the \code{study_id} column.}
}
\value{
\code{metadata} with one column per parameter appended; \code{url_query} stays.
}
\description{
The \code{url_query} column of a metadata tibble holds, per study result, the
query parameters the worker arrived with, which for a Prolific study link
are \code{PROLIFIC_PID}, \code{STUDY_ID} and \code{SESSION_ID}. This turns them into one
character column per parameter, named in snake case with \code{prefix} in
front (\code{query_prolific_pid}, \code{query_study_id}, \code{query_session_id}), so
that the tibble joins to a Prolific export without unnesting. Study
results without a parameter get \code{NA}; a tibble without any parameter is
returned unchanged. Two parameters that differ only in case or
separators (\code{PROLIFIC_PID} and \code{prolific_pid}) would share a column
name; the second gets a numeric suffix and a warning names both keys.
}
\examples{
meta <- jatos_flatten_metadata(
  system.file("extdata", "metadata.json", package = "jatosr")
)
jatos_url_query(meta)[, c("study_result_id", "query_prolific_pid", "query_session_id")]
}
