% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/result-files.R
\name{jatos_result_files}
\alias{jatos_result_files}
\title{Files uploaded during a run, one row per file}
\usage{
jatos_result_files(metadata)
}
\arguments{
\item{metadata}{A metadata tibble from \code{\link[=jatos_results_metadata]{jatos_results_metadata()}},
\code{\link[=jatos_read_metadata]{jatos_read_metadata()}} or \code{\link[=jatos_flatten_metadata]{jatos_flatten_metadata()}}. Only its id
columns and \code{files} are used.}
}
\value{
A tibble with \code{study_result_id}, \code{component_result_id},
\code{component_id}, \code{batch_id}, \code{filename} and \code{size} (bytes on the server).
}
\description{
Widens the \code{files} list column of a metadata tibble into one row per
attached file, so that what a study's participants uploaded (drawings,
audio recordings, anything sent with \code{jatos.uploadResultFile()}) can be
listed, filtered and planned before a download. Component results
without files contribute no row.
}
\examples{
meta <- jatos_flatten_metadata(
  system.file("extdata", "metadata.json", package = "jatosr")
)
jatos_result_files(meta)
}
\seealso{
\code{\link[=jatos_download_files]{jatos_download_files()}} to fetch them.
}
