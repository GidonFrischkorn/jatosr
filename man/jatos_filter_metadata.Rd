% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata-filter.R
\name{jatos_filter_metadata}
\alias{jatos_filter_metadata}
\title{Keep the study results that belong in a dataset}
\usage{
jatos_filter_metadata(
  metadata,
  states = NULL,
  worker_types = NULL,
  since = NULL,
  until = NULL,
  exclude_study_result_id = NULL,
  exclude_worker_id = NULL,
  tz = "UTC"
)
}
\arguments{
\item{metadata}{A metadata tibble from \code{\link[=jatos_results_metadata]{jatos_results_metadata()}} or
\code{\link[=jatos_read_metadata]{jatos_read_metadata()}}.}

\item{states}{Study states to keep, for example \code{"FINISHED"}. \code{NULL}
keeps every state. JATOS states are \code{PRE}, \code{STARTED}, \code{DATA_RETRIEVED},
\code{FINISHED}, \code{ABORTED} and \code{FAIL}.}

\item{worker_types}{Worker types to keep, for example
\code{c("PersonalSingle", "GeneralMultiple")}. \code{NULL} keeps every type; the
GUI test runs are \code{"Jatos"}.}

\item{since}{Keep study results that started at or after this time: a
\code{POSIXct}, a \code{Date}, or a string such as \code{"2025-08-24"} or
\code{"2025-08-24 12:00:00"}, read in the time zone \code{tz}. Rows without a
start time are dropped when \code{since} is given.}

\item{until}{Keep study results that started \emph{before} this time, given
like \code{since}; \code{since} and \code{until} together select a half-open
interval, so \verb{since = "2025-08-24", until = "2025-08-25"} is one day.
Rows without a start time are dropped when \code{until} is given.}

\item{exclude_study_result_id, exclude_worker_id}{Study result ids, or
worker ids, whose study results are dropped; for the researcher's own
runs through a real link, which \code{worker_types} cannot tell apart.}

\item{tz}{Time zone in which date strings in \code{since} and \code{until} are
read, and in which the message shows them. \code{"UTC"} by default, the
zone the server reports times in; \code{Sys.timezone()} for local time.}
}
\value{
\code{metadata} without the dropped rows.
}
\description{
Drops rows of a metadata tibble by study state, worker type, start time
and explicit exclusions, the filters every export makes before the
trials are read: unfinished runs (\code{study_state != "FINISHED"}), the
researcher's own test runs from the JATOS GUI (\code{worker_type == "Jatos"}), runs before the study went live or after a wave closed, and
known test runs by their study result or worker id. The filters work on
the study result, the run of one participant, so every component result
of a dropped run goes with it. A message reports how many study results
each filter removed; no message when no filter is given.
}
\examples{
meta <- jatos_flatten_metadata(
  system.file("extdata", "metadata.json", package = "jatosr")
)
jatos_filter_metadata(meta, states = "FINISHED", since = "2025-08-24")
jatos_filter_metadata(meta, until = "2025-08-24 02:00", exclude_worker_id = 503)
}
\seealso{
\code{\link[=jatos_export_results]{jatos_export_results()}}, which applies the same filters
before the download.
}
