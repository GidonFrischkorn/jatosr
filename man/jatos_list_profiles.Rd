% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile.R
\name{jatos_list_profiles}
\alias{jatos_list_profiles}
\title{List the credential profiles on this machine}
\usage{
jatos_list_profiles()
}
\value{
A tibble with one row per profile: \code{profile} (lower-case name),
\code{host} (\code{NA} when no host is known), \code{has_token}, \code{active} (\code{TRUE} for
the profile that \code{\link[=jatos_connection]{jatos_connection()}} uses when called without a
\code{profile} argument, from \code{JATOS_PROFILE} or \code{"default"}), and
\code{auth_from}, which of \code{"env"} or \code{"keyring"} a token would be taken
from. The default profile comes first, the rest are sorted by name.
Zero rows when nothing is set.

\code{active} is \code{FALSE} in \emph{every} row on a machine that holds named
profiles only: there is no unsuffixed \code{JATOS_HOST} / \code{JATOS_TOKEN}
pair and \code{JATOS_PROFILE} is unset, so nothing is selected and a bare
\code{jatos_connection()} has no credentials to read. That is a valid
setup, not a fault; name a profile with \code{jatos_connection("<name>")},
or set \code{JATOS_PROFILE} to make one of them active.
}
\description{
A profile is a named pair of a host and a token, so that one machine can
hold credentials for several accounts on one server, or for several
servers, side by side. The profile \code{"default"} is the one used when none
is named.
}
\details{
Profiles are collected from all three places the package reads: the
environment variables of this session (\verb{JATOS_HOST_<PROFILE>} and
\verb{JATOS_TOKEN_<PROFILE>}, or the unsuffixed pair for \code{"default"}), the
profile configuration file written by \code{\link[=jatos_set_credentials]{jatos_set_credentials()}}, and the
system credential store. It makes no request, retrieves no secret, and
never returns a token — the credential store is asked for its entry names
only, so nothing is unlocked.
}
\examples{
\dontrun{
# lists the machine's credential store
jatos_list_profiles()
}
}
