% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-archive.R
\name{jatos_export_archive}
\alias{jatos_export_archive}
\title{Download the results archive as the server produces it}
\usage{
jatos_export_archive(
  study_id = NULL,
  batch_id = NULL,
  file = NULL,
  overwrite = FALSE,
  conn = jatos_connection()
)
}
\arguments{
\item{study_id, batch_id}{One or more ids to select results by; at least
one of the two must be given. \code{study_id} may also be one or more uuid
strings. Several are combined as the server combines them (the union).}

\item{file}{Path to write, conventionally with the extension \code{.zip}.
Its directory must exist. \code{NULL} takes the server's file name in the
working directory.}

\item{overwrite}{If \code{TRUE}, an existing file is replaced.}

\item{conn}{A \code{\link[=jatos_connection]{jatos_connection()}}.}
}
\value{
The path written, invisibly; a message reports it with the size.
}
\description{
Fetches the zip that \code{POST /results} builds for the studies or batches
given (every result's \code{data.txt}, the uploaded files and one
\code{metadata.json} at the root, the same archive the JATOS GUI's "Export
Results" produces) and writes it to \code{file} untouched. This is an
archival verb: for a data deposit the artefact people trust is the zip
JATOS itself made, not a tibble. It is never the incremental path; the
server re-sends every result each time. \code{\link[=jatos_import_results]{jatos_import_results()}} turns
such a zip into a cache.
}
\details{
The archive is written under a temporary name in the target directory
and renamed into place after the answer was checked to be a zip file;
an existing \code{file} is never replaced unless \code{overwrite = TRUE}. Without
\code{file}, the archive lands in the working directory under the name the
server sends (what a browser would save from the GUI's export), reduced
to its base name; \code{jatos_results.zip} when the server names none. A file
in the way is then found only after the answer, so a run that is refused
has already downloaded the archive once.
}
\examples{
\dontrun{
# needs a JATOS server and a stored API token
jatos_export_archive(study_id = 12, file = "data/study12_results.zip")
jatos_export_archive(study_id = 12) # the server's name, here
}
}
\seealso{
\code{\link[=jatos_export_study]{jatos_export_study()}} for the study archive,
\code{\link[=jatos_export_results]{jatos_export_results()}} whose \code{archive_results = TRUE} writes this
archive next to the dataset.
}
