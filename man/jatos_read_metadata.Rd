% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata-local.R
\name{jatos_read_metadata}
\alias{jatos_read_metadata}
\title{Rebuild result metadata from a local cache}
\usage{
jatos_read_metadata(path, batch_id = NULL)
}
\arguments{
\item{path}{The cache directory, one \verb{batch_<id>} directory inside it, or
the path of a single \code{metadata.json} file.}

\item{batch_id}{Optional ids to keep only some batches.}
}
\value{
A metadata tibble as documented in \code{\link[=jatos_flatten_metadata]{jatos_flatten_metadata()}},
with \code{file} (path of the local \code{data.txt}, \code{NA} when it has not been
downloaded) and \code{file_size} (its size in bytes, \code{NA} when absent).
}
\description{
Reads the \code{metadata.json} files that \code{jatos_download_results()} stores in
\verb{<path>/batch_<id>/} and returns the same tibble that
\code{\link[=jatos_results_metadata]{jatos_results_metadata()}} returned when they were fetched, plus two
columns describing the data file on disk. No network is needed, so a
pipeline can be replayed offline. Each batch is read once, from its own
\verb{batch_<id>/metadata.json}; a directory in another layout (a
\code{metadata.json} at the top level, \verb{JATOS_DATA_<id>} folders) is refused,
see \code{\link[=jatos_download_results]{jatos_download_results()}}.
}
\examples{
cache <- system.file("extdata", "JATOS_data", package = "jatosr")
meta <- jatos_read_metadata(cache)
meta[, c("study_result_id", "component_result_id", "data_size", "file_size")]
meta[is.na(meta$file) & meta$data_size > 0, ]   # still to download: none
}
\seealso{
\code{\link[=jatos_cache_status]{jatos_cache_status()}} for a per-batch summary of the same cache.
}
